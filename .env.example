# TaskChampion Sync Server Configuration
# Optional: Restrict access to specific client IDs (comma-separated list)
# Leave empty to allow all client IDs to connect
# Example: ALLOW_CLIENT_IDS=85038910-8fe2-480d-b6cb-6e7fabc1fa44,another-uuid-here
ALLOW_CLIENT_IDS=

# Inky Bridge Configuration
# URL of the TaskChampion sync server
# For local Docker: http://sync-server:8080
# For external access: https://your-sync-server.example.com (via reverse proxy)
TASKCHAMPION_SYNC_SERVER_URL=http://sync-server:8080

# Client ID for the inky-bridge replica
# IMPORTANT: This must match the client_id used by your Taskwarrior clients
# All devices syncing the same tasks must use the SAME client_id
# Generate with: uuidgen
# Example: 85038910-8fe2-480d-b6cb-6e7fabc1fa44
TASKCHAMPION_CLIENT_ID=your-bridge-client-id

# Encryption secret (stored in Docker secret file)
# DO NOT put this in .env - use Docker secrets instead
# IMPORTANT: This must match the encryption_secret used by your Taskwarrior clients
# All devices syncing the same tasks must use the SAME encryption_secret
# Generate with: pwgen -s 64 1 (or use a password manager)
# Store in: secrets/taskchampion_encryption_secret.txt
# See docker-compose.yml secrets section
# TASKCHAMPION_ENCRYPTION_SECRET=your-encryption-secret

# Data directory for replica storage (default: /data/replica)
DATA_DIR=/data/replica

# Sync timeout in seconds (default: 30)
# Maximum time to wait for sync operation to complete
SYNC_TIMEOUT_SECONDS=30

# Minimum sync interval in seconds (default: 10)
# Prevents rapid repeated syncs - sync will be skipped if last sync
# was within this interval
MIN_SYNC_INTERVAL_SECONDS=10

# Optional: API authentication secret
# If set, all API requests must include: Authorization: Bearer <secret>
# Leave empty to disable authentication
AUTH_SECRET=
